module.exports = {
  name: "antilink-group",
  description:
    "Antilink group. when there is a link in the group it will be automatically deleted.",
  run: async ({ m, sock }) => {
    return (next = true);
  },
};
